import { render } from '@testing-library/react';
import React from 'react';

export default class FooterContent extends React.Component{
    render(){
        return (
            <div>
                this is footer coentent
            </div>
        )
    }
}