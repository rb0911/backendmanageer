import { render } from '@testing-library/react';
import React from 'react';

export default class NavLeft extends React.Component{
    render(){
        return (
            <div>
                this is left coentent
            </div>
        )
    }
}